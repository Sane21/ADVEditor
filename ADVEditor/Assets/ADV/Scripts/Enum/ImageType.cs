namespace HarapekoADV.Images
{
    /// <summary>
    /// Character 表示とアニメーション、表情差分などを入れれるようにする(未実装)
    /// Item 表示とアニメーションのみ
    /// Background 背景の表示のみ
    /// ERR 例外エラー
    /// </summary>
    public enum ImageType
    {
        Characetr,
        Item,
        Background,
        ERR
    }
}